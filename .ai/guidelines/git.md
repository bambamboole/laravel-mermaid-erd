# Git & Pull Requests

- Releases are cut by release-please from the conventional-commit subjects: a `feat:` bumps the
  minor version, a `fix:` the patch, and a `BREAKING CHANGE:` footer marks a breaking change.
- For changes to the diagram output, include a before/after snippet of the generated Mermaid in the
  PR description.
